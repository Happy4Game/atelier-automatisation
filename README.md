# SonarQube avec Terraform

Voici les commandes à exécuter:

```bash
# Initialiser le projet terraform
terraform init

# Déployer avec terraform en local
terraform apply

# Supprimer toute trace de sonarqube
terraform destroy
```
